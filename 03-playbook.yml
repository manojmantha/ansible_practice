- name: Install and Run Nginx
  hosts: local
  connection: local
  become: yes
  tasks:
  - name: install Nginx
    ansible.builtin.package:
      name: nginx
      state: present

  - name: Run nginx
    ansible.builtin.service:
      name: nginx
      state: started
      enabled: yes